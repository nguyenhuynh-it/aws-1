version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo Installing dependencies...
       # - pip install -r requirements.txt

  build:
    commands:
      - echo Build started on `date`
      - echo Running build scripts...
      - echo ========
      # Thêm lệnh build ở đây nếu có

artifacts:
  files:
    - '**/*'
